const a = [1, 2, 3];

const sum = (a: number[]) => a.reduce((t, x) => t + x, 0);

const doWork = () => {
  log(sum(a));
};

log(defines.alert_type.entity_under_attack);
game.auto_save("ok");
game.connected_players.forEach((v) => localised_print(v));
script.on_event(
  "ar",
  () => {
    return "k";
  },
  null
);
doWork();
